{% for sector, sonda in sonde %}
    <div class="col text-center">
        <a href="#" class="badge badge-secondary" onclick="SONDE.getSondeBySector({{ sector }})">Sector {{ sector }}</a>
    </div>
{% endfor %}