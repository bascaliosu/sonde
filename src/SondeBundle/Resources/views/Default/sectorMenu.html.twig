{% for sector, sonda in sonde %}
    <div class="col text-center">
        <a href="#" class="btn btn-secondary" onclick="SONDE.getSondeBySector({{ sector }})">Sector {{ sector }}</a>
    </div>
{% endfor %}